<RelativeLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
        android:id="@+id/rl_group_chat"
        android:layout_width="fill_parent"
        android:layout_height="fill_parent"
        android:orientation="vertical" >
<!-- 
        <ListView
            android:id="@+id/lsit_address"
            android:layout_width="fill_parent"
            android:layout_height="wrap_content"
            android:cacheColorHint="@android:color/transparent"
            android:divider="@null"
            android:fadingEdge="none"
            android:listSelector="@android:color/transparent" >
        </ListView> -->
    <android.support.v7.widget.RecyclerView
        android:id="@+id/rv"
        android:layout_width="fill_parent"
        android:layout_height="fill_parent"
        android:background="@android:color/white"/>
        <TextView
            android:id="@+id/show_letter"
            android:layout_width="70dip"
            android:layout_height="50dip"
            android:layout_centerInParent="true"
            android:background="@drawable/shape_rect_gray_buttom"
            android:gravity="center"
            android:text="A"
            android:textColor="#6FCD78"
            android:textSize="30sp"
            android:textStyle="bold"
             android:visibility="gone" />

        <com.example.widget.SideBar
            android:id="@+id/sidebar"
            android:layout_width="24dip"
            android:layout_height="fill_parent"
            android:layout_alignParentRight="true"
            android:layout_below="@+id/query_phone_rl" />
    </RelativeLayout>
